/**
 * finite-state-language/react — the useMachine hook (spec §7.1).
 *
 * M0 placeholder: the adapter lands in M5. This subpath is excluded from
 * the core bundle; react is an optional peer dependency.
 */

export {};
